# Undo-záloha stavu — transientní, vzniká při každém zápisu (mini undo si
# pamatuje jeden krok zpět). Do gitu nepatří; pro velké projekty by navíc
# zbytečně bobtnala historie.
phases-prev/
state.prev.json

# Transientní artefakty atomického zápisu grafu (mini map). Vznikají v půli
# přegenerování a hned se přejmenují/uklidí; do gitu nikdy nepatří. Zůstat by
# mohly jen po pádu uprostřed zápisu.
graph.tmp/
graph.json.tmp

# Programově generované/odvozené výstupy — jdou kdykoli znovu vytvořit ze zdroje
# pravdy (zdrojáky / .mini/memory/), tak ať nedělají šum v diffu a commitech.
# Strojová mapa projektu (mini map / done) — derivace zdrojáků.
graph/
graph.json
# Žebříček příkazů podle tokenů — čistý report z měření.
token-report.md
# Zeštíhlené shrnutí poslední fáze pro `next` — odvozené z .mini/memory/.
last-memory.md

# Swap/backup soubory editorů (vim apod.) — vznikají při editaci, do gitu nepatří.
*.sw[a-p]
*~
